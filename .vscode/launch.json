{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "launch: headergen",
            "type": "lldb",
            "request": "launch",
            "program": "${workspaceFolder}/build/headergen",
            "args": ["./src", "./headergen", "./tests", "--pub"],

            "cwd": "${workspaceFolder}",

            "preLaunchTask": "make: headergen"
        }
    ]
}